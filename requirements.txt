pandas
numpy
matplotlib
seaborn
scikit-learn
ipykernel

# Optional (specific notebooks):
#   pip install torch      # 12 — autoencoder; 23 — sequence model
#   pip install lightgbm   # 19 — optional model family in bakeoff
#   pip install xgboost    # 17 — optional comparison cell
